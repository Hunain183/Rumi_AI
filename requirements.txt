# ─── RUMI Dependencies ─────────────────────────────────────
# Core
openai>=1.0.0             # LLM connection (works with Ollama, OpenAI, etc.)
rich>=13.0.0              # Beautiful terminal UI

# Desktop GUI (optional)
PyQt6>=6.0.0              # Desktop application framework
PyQt6-WebEngine>=6.0.0    # Web rendering engine

# Voice (optional — RUMI works in text mode without these)
SpeechRecognition>=3.10.0 # Microphone input
pyttsx3>=2.90             # Text-to-speech output
PyAudio>=0.2.14           # Audio hardware interface

# System & Monitoring
psutil>=5.9.0             # CPU, RAM, disk, process monitoring

# Web
requests>=2.31.0          # HTTP requests
beautifulsoup4>=4.12.0    # Web page parsing
duckduckgo-search>=6.0    # Web search (no API key needed)

# Scheduling
schedule>=1.2.0           # Task scheduling
